export default {
  "id": 685,
  "name": "Redundant Connection II",
  "difficulty": "hard",
  "premium": false,
  "topic": "algorithms",
  "url": "https://leetcode.com/problems/redundant-connection-ii",
  "relativeDir": "R/Redundant Connection II",
  "slug": "0685-redundant-connection-ii",
  "availableLanguages": [
    "cpp",
    "java",
    "python"
  ],
  "defaultLanguage": "cpp",
  "lineCounts": {
    "cpp": 63,
    "java": 76,
    "python": 74
  },
  "languages": {
    "cpp": "// Runtime: 22 ms (Top 29.21%) | Memory: 9.9 MB (Top 41.59%)\r\nclass Solution {\r\npublic:\r\n    /*\r\n    There are two cases for the tree structure to be invalid.\r\n    1) A node having two parents;\r\n    2) A circle exists\r\n\r\n    If there are both invalid conditions, which means there is a node which has 2 parents and there is also a circle even after we invalid edgeB.\r\n    In this case, we have to return edgeA. only in this way can we avoid both double parents and circle for the tree.\r\n    */\r\n\r\n    int find(vector<int>& par, int n){\r\n        if(par[n]==n || par[n]<0) return n;\r\n        par[n] = find(par, par[n]);\r\n        return par[n];\r\n    }\r\n\r\n    void findSpecialEdge(vector<vector<int>>& edges, vector<int>& edgeA, vector<int>& edgeB){\r\n        int len = edges.size();\r\n        vector<int>par(len+1, 0);\r\n        for(vector<int> e : edges){\r\n            int p =e[0], c=e[1];\r\n            if(par[c]){\r\n                edgeA = {par[c], c};\r\n                edgeB = e;\r\n                return;\r\n            }else{\r\n                par[c] = p;\r\n            }\r\n        }\r\n    }\r\n\r\n    vector<int> findRedundantDirectedConnection(vector<vector<int>>& edges) {\r\n        int len = edges.size();\r\n        vector<int>parent(len+1, 0);\r\n        for(int i=0; i<=len; i++) parent[i]=i;\r\n\r\n        vector<int>edgeA={}; // 1st candidate\r\n        vector<int>edgeB={}; // 2nd candidate\r\n        findSpecialEdge(edges, edgeA, edgeB);\r\n\r\n        for(vector<int> e : edges){\r\n            int n1 = e[0];\r\n            int n2 = e[1];\r\n            if(edgeB.size()>0 && (edgeB[0]==n1 && edgeB[1]==n2)){ // invalidate edgeB\r\n                continue;\r\n            }\r\n            int p1 = find(parent, n1);\r\n            int p2 = find(parent, n2);\r\n            if(p1==p2){ // circle found\r\n                if(edgeB.size()>0){\r\n                    return edgeA;\r\n                }else{\r\n                    return e;\r\n                }\r\n            } else{\r\n                parent[p2] = p1;\r\n            }\r\n        }\r\n        return edgeB;\r\n    }\r\n};",
    "python": "# Runtime: 118 ms (Top 30.98%) | Memory: 14.6 MB (Top 46.10%)\r\nclass Solution:\r\n    def findRedundantDirectedConnection(self, edges: List[List[int]]) -> List[int]:\r\n        # THREE DIFFERENT TYPES OF REDUNDANT TREES CAN EXISIT IDENTIFY THOSE (CYCLE,NOCYCLE,INDEGREE2)\r\n        # CAN BE SOLVED USING DSU OR DFS\r\n        class DSU:\r\n            def __init__(self,n):\r\n                self.parent = [i for i in range(1005)]\r\n            def find(self,node):\r\n                if self.parent[node] == node:\r\n                    return node\r\n                self.parent[node] = self.find(self.parent[node])\r\n                return self.parent[node]\r\n            def union(self,node1,node2):\r\n                p1 = self.find(node1)\r\n                p2 = self.find(node2)\r\n                if p1 != p2:\r\n                    self.parent[p1] = p2\r\n            def isConnected(self,node1,node2):\r\n                return self.find(node1) == self.find(node2)\r\n        def isValidTree(edges,edge,n):\r\n            d = DSU(n)\r\n            for e in edges:\r\n                if e == edge:\r\n                    continue\r\n                d.union(e[0],e[1])\r\n            return d.isConnected(edge[0],edge[1])\r\n\r\n        indegree = []\r\n        count = defaultdict(int)\r\n        for i,j in edges:\r\n            count[j] = count.get(j,0)+1\r\n        n = len(edges)\r\n        for i in range(n):\r\n            if count[edges[i][1]] == 2:\r\n                indegree += [i]\r\n        if len(indegree) != 0:\r\n            if isValidTree(edges,edges[indegree[-1]],n):\r\n                return edges[indegree[-1]]\r\n            return edges[indegree[0]]\r\n        else:\r\n            d2 = DSU(n)\r\n            for e in edges:\r\n                if d2.isConnected(e[0],e[1]):\r\n                    return e\r\n                d2.union(e[0],e[1])\r\n\r\n# def dfs(node):\r\n# if node in seen:\r\n# return False\r\n# seen.add(node)\r\n# for nb in g[node]:\r\n# if not dfs(nb):\r\n# return False\r\n# return True\r\n# g = defaultdict(list)\r\n# v = defaultdict(int)\r\n# total = set()\r\n# for i,j in edges:\r\n# g[i] = g.get(i,[]) + [j]\r\n# v[j] = v.get(j,0) + 1\r\n# total.add(i)\r\n# total.add(j)\r\n# for e in edges[::-1]:\r\n# g[e[0]].remove(e[1])\r\n# v[e[1]] -= 1\r\n# for root in total:\r\n# seen = set()\r\n# if v[root] == 0 and dfs(root) and len(seen) == len(total):\r\n# return e\r\n\r\n# v[e[1]] += 1\r\n# g[e[0]].append(e[1])\r\n# return [-1,-1]",
    "java": "// Runtime: 3 ms (Top 24.18%) | Memory: 44.9 MB (Top 16.79%)\r\nclass Solution {\r\n    int[] dsu;\r\n    public int[] findRedundantDirectedConnection(int[][] edges) {\r\n        int n=edges.length;\r\n        int[] parent=new int[n+1];\r\n        Arrays.fill(parent,-1);\r\n\r\n        int[] e2=null;\r\n        int[] e1=null;\r\n        boolean twopt=false;\r\n\r\n        for(int[] edge: edges){\r\n\r\n            int from=edge[0];\r\n            int to=edge[1];\r\n\r\n            if(parent[to]==-1){\r\n                parent[to]=from;\r\n            }else{\r\n                twopt=true;\r\n                e2=edge;\r\n                e1=new int[]{parent[to],to};\r\n                break;\r\n            }\r\n        }\r\n\r\n        dsu=new int[edges.length+1];\r\n        for(int i=0;i<=edges.length;i++){\r\n            dsu[i]=i;\r\n        }\r\n        if(twopt==false){\r\n            int[] res=null;\r\n\r\n            for(int[] edge: edges){\r\n                int from=edge[0];\r\n                int to=edge[1];\r\n\r\n                int fromlead=find(from);\r\n                if(fromlead==to){\r\n                    res=edge;\r\n                    break;\r\n                }else{\r\n                    dsu[to]=fromlead;\r\n                }\r\n            }\r\n            return res;\r\n        }else{\r\n            boolean iscycle=false;\r\n            for(int[] edge: edges){\r\n                if(edge==e2) continue;\r\n                int from =edge[0];\r\n                int to=edge[1];\r\n\r\n                int fromlead=find(from);\r\n\r\n                if(fromlead==to){\r\n                    iscycle=true;\r\n                    break;\r\n                }else{\r\n                    dsu[to]=fromlead;\r\n                }\r\n            }\r\n            if(iscycle==true){\r\n                return e1;\r\n            }else{\r\n                return e2;\r\n            }\r\n        }\r\n\r\n    }\r\n    public int find(int x){\r\n        if(dsu[x]==x) return x;\r\n        return dsu[x]=find(dsu[x]);\r\n    }\r\n}"
  }
}
