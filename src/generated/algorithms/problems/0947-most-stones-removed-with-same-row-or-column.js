export default {
  "id": 947,
  "name": "Most Stones Removed with Same Row or Column",
  "difficulty": "medium",
  "premium": false,
  "topic": "algorithms",
  "url": "https://leetcode.com/problems/most-stones-removed-with-same-row-or-column",
  "relativeDir": "M/Most Stones Removed with Same Row or Column",
  "slug": "0947-most-stones-removed-with-same-row-or-column",
  "availableLanguages": [
    "cpp",
    "java",
    "python",
    "javascript"
  ],
  "defaultLanguage": "cpp",
  "lineCounts": {
    "cpp": 61,
    "java": 77,
    "python": 26,
    "javascript": 48
  },
  "languages": {
    "cpp": "class Solution {\r\n    class UnionFind \r\n    {\r\n        vector<int> parent, rank;\r\n        public:\r\n        int count = 0;\r\n        UnionFind(int n)\r\n        {\r\n            count = n;\r\n            parent.assign(n, 0);\r\n            rank.assign(n, 0);\r\n            for(int i=0;i<n;i++)\r\n            {\r\n                parent[i] = i;\r\n            }\r\n        }\r\n        int find(int p)\r\n        {\r\n            while(p!=parent[p])\r\n            {\r\n                parent[p] = parent[parent[p]];\r\n                p = parent[p];\r\n            }\r\n            return p;\r\n        }\r\n        bool Union(int p, int q)\r\n        {\r\n            int rootp = find(p);\r\n            int rootq = find(q);\r\n            if(rootp == rootq)\r\n                return false;\r\n            else if(rank[rootp] < rank[rootq])\r\n            {\r\n                parent[rootp] = rootq;\r\n                rank[rootq]++;\r\n                count--;\r\n                return true;\r\n            }\r\n            else\r\n            {\r\n                parent[rootq] = rootp;\r\n                rank[rootp]++;\r\n                count--;\r\n                return true;\r\n            }\r\n        }\r\n    };\r\npublic:\r\n    int removeStones(vector<vector<int>>& stones) {\r\n        int n = stones.size();\r\n        UnionFind uf(n);\r\n        for(int i=0;i<n;i++){\r\n            for(int j=0;j<i;j++){\r\n                if(stones[i][0] == stones[j][0] or stones[i][1] == stones[j][1])\r\n                    uf.Union(i,j);\r\n            }\r\n        }\r\n\r\n        return n - uf.count;\r\n    }\r\n};",
    "python": "# Runtime: 187 ms (Top 84.32%) | Memory: 18.9 MB (Top 5.06%)\r\nclass Solution:\r\n    def removeStones(self, stones: List[List[int]]) -> int:\r\n        def dfs(row,col):\r\n            if seen[(row,col)]:\r\n                return 0\r\n            seen[(row,col)] = True\r\n            for r,c in tableRow[row]:\r\n                dfs(r,c)\r\n            for r,c in tableCol[col]:\r\n                dfs(r,c)\r\n            return 1\r\n\r\n        tableRow, tableCol = {},{}\r\n        for row,col in stones:\r\n            if row not in tableRow:\r\n                tableRow[row] = set()\r\n            if col not in tableCol:\r\n                tableCol[col] = set()\r\n            tableRow[row].add((row,col))\r\n            tableCol[col].add((row,col))\r\n\r\n        count,seen= 0, {(row,col):False for row,col in stones}\r\n        for row,col in stones:\r\n            count += dfs(row,col)\r\n        return abs(len(stones)-count)",
    "java": "// Runtime: 70 ms (Top 33.32%) | Memory: 48.7 MB (Top 77.91%)\r\nclass Solution {\r\n    public int removeStones(int[][] stones) {\r\n        int ret=0;\r\n        DisjointSet ds=new DisjointSet(stones.length);\r\n\r\n        for(int i=0;i<stones.length;i++) {\r\n            for(int j=i+1;j<stones.length;j++) {\r\n                int s1[]=stones[i];\r\n                int s2[]=stones[j];\r\n\r\n                if(s1[0]==s2[0] || s1[1]==s2[1]) {\r\n                    ds.union(i, j);\r\n                }\r\n            }\r\n        }\r\n        // System.out.println(Arrays.toString(ds.sets));\r\n        for(int i=0;i<ds.sets.length;i++) {\r\n            if(ds.sets[i]<0)\r\n                ret+=Math.abs(ds.sets[i])-1;\r\n        }\r\n\r\n        return ret;\r\n    }\r\n\r\n    class DisjointSet {\r\n        public int sets[];\r\n\r\n        public DisjointSet(int size) {\r\n            sets=new int[size];\r\n            Arrays.fill(sets, -1);\r\n        }\r\n\r\n        //\r\n        // weighted union\r\n        //union->return size in negative\r\n        public int union(int idx1, int idx2) {\r\n            int p1=find(idx1);\r\n            int p2=find(idx2);\r\n\r\n            if(p1==p2) { //same parent so directly returning size\r\n                return sets[p1];\r\n            }else {\r\n                int w1=Math.abs(sets[p1]);\r\n                int w2=Math.abs(sets[p2]);\r\n\r\n                if(w1>w2) {\r\n                    sets[p2]=p1;\r\n\r\n                    //collapsing FIND\r\n                    sets[idx1]=p1;\r\n                    sets[idx2]=p1;\r\n\r\n                    return sets[p1]=-(w1+w2);\r\n                }else {\r\n                    sets[p1]=p2;\r\n\r\n                    //collapsing FIND\r\n                    sets[idx1]=p2;\r\n                    sets[idx2]=p2;\r\n\r\n                    return sets[p2]=-(w1+w2);\r\n                }\r\n            }\r\n        }\r\n\r\n        // collapsing FIND\r\n        //find parent\r\n        public int find(int idx) {\r\n            int p=idx;\r\n            while(sets[p]>=0) {\r\n                p=sets[p];\r\n            }\r\n            return p;\r\n        }\r\n    }\r\n}",
    "javascript": "/**\r\n * @param {number[][]} stones\r\n * @return {number}\r\n */\r\nvar removeStones = function(stones) {\r\n    \r\n    const n = stones.length\r\n    // initial number of components(a.k.a island)\r\n    let numComponents = n\r\n    \r\n    //initial forest for union find\r\n    let forest = new Array(n).fill(0).map((ele, index) => index)\r\n\r\n    // recursively finding the root of rarget node\r\n    const find = (a) => {\r\n        if(forest[a] === a) {\r\n            return a\r\n        }\r\n        return find(forest[a])\r\n    }\r\n    \r\n    // function for uniting two stones\r\n    const union = (a, b) => {\r\n        const rootA = find(a)\r\n        const rootB = find(b)\r\n        if(rootA != rootB) {\r\n            //connect their roots if currently they are not connected\r\n            forest[rootA] = rootB\r\n            //subtract the number of islands by one since two islands are connected now\r\n            numComponents -= 1\r\n        }\r\n    }\r\n    \r\n    for(let i = 0; i < stones.length - 1; i++) {\r\n        for(let j = i + 1; j < stones.length; j++) {\r\n            // if two stones are connected(i.e. share same row or column), unite them\r\n            if(stones[i][0] === stones[j][0] || stones[i][1] === stones[j][1]) {\r\n                union(i, j)\r\n            }\r\n        }\r\n    }\r\n\r\n    // this is the most confusing part.\r\n    // The number of stones can be removed is not equal to the number of islands.\r\n    // e.g. we have two islands with total of 10 stores, each island will leave one extra stone after the \r\n    // removal, therefore we can remove 10 - 2 = 8 stones in total\r\n    return n - numComponents\r\n};"
  }
}
