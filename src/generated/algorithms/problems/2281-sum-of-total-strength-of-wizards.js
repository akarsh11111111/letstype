export default {
  "id": 2281,
  "name": "Sum of Total Strength of Wizards",
  "difficulty": "hard",
  "premium": false,
  "topic": "algorithms",
  "url": "https://leetcode.com/problems/sum-of-total-strength-of-wizards",
  "relativeDir": "S/Sum of Total Strength of Wizards",
  "slug": "2281-sum-of-total-strength-of-wizards",
  "availableLanguages": [
    "cpp",
    "java",
    "python"
  ],
  "defaultLanguage": "cpp",
  "lineCounts": {
    "cpp": 55,
    "java": 67,
    "python": 32
  },
  "languages": {
    "cpp": "class Solution {\r\npublic:\r\n    int mod=1e9+7;\r\n    int Value(vector<int> a, int n){\r\n        vector<long long>pre(n+2,0);\r\n        for(int i=1;i<=n;i++)\r\n            pre[i]=(pre[i-1]+a[i-1])%mod;\r\n        for(int i=1;i<=n+1;i++)\r\n            pre[i]=(pre[i-1]+pre[i])%mod;\r\n        for(int i=n+1;i>0;i--)\r\n            pre[i]=pre[i-1];\r\n\r\n        vector<int> l(n,-1),r(n,n);\r\n        stack<int> st;\r\n        // Find all left index\r\n        for(int i = 0; i < n; i++){\r\n            while (!st.empty() && a[st.top()] >= a[i])\r\n                st.pop();\r\n            if (!st.empty())\r\n                l[i] = st.top();\r\n\r\n            st.push(i);\r\n        }\r\n\r\n        while(!st.empty())\r\n        st.pop();\r\n\r\n        // Find all right index\r\n        for(int i = n - 1; i >= 0; i--){\r\n            while (!st.empty() && a[st.top()] > a[i])\r\n                st.pop();\r\n\r\n                if (!st.empty())\r\n                    r[i] = st.top();\r\n\r\n            st.push(i);\r\n        }\r\n        // for(int i=0;i<n;i++) cout<<a[i]<<\" \"; cout<<\"\\n\";\r\n        // for(int i=0;i<n;i++) cout<<l[i]<<\" \"; cout<<\"\\n\";\r\n        // for(int i=0;i<n;i++) cout<<r[i]<<\" \"; cout<<\"\\n\";\r\n        \r\n        long long ans = 0;\r\n    for (int i = 0; i < n; ++i) {\r\n        ans += (((pre[r[i] + 1] - pre[i + 1]) * (i - l[i]) % mod + mod * 2 - \r\n               (pre[i + 1] - pre[l[i] + 1]) * (r[i] - i) % mod) % mod * a[i]) % mod;\r\n        ans %= mod;\r\n    }\r\n    return (int) ans;\r\n\r\n    }\r\n    int totalStrength(vector<int>& s) {\r\n        int n=s.size();\r\n        return Value(s,n);\r\n    }\r\n};",
    "python": "class Solution:\r\n    def totalStrength(self, stp: List[int]) -> int:\r\n        st = []\r\n        n = len(stp)\r\n        m1 = defaultdict(lambda:-1)\r\n        ps = [0]\r\n        for i in range(n):\r\n            while st and stp[st[-1]] >= stp[i]:\r\n                st.pop()\r\n            if st: m1[i] = st[-1]\r\n            st.append(i)\r\n            ps.append(ps[-1] + stp[i])\r\n        pss = [0]\r\n        for i in ps:\r\n            pss.append(pss[-1] + i)\r\n        st = []\r\n        m2 = defaultdict(lambda:n)\r\n        for i in range(n-1,-1,-1):\r\n            while st and stp[st[-1]] > stp[i]:\r\n                st.pop()\r\n            if st: m2[i] = st[-1]\r\n            st.append(i)\r\n\r\n        ans = 0\r\n        mod = 10**9 + 7\r\n        for i in range(n):\r\n            left = m1[i] + 1\r\n            right = m2[i]\r\n            p1 = (i+1-left)*(pss[right+1]-pss[i+1])\r\n            p2 = (right-i)*(pss[i+1]- pss[left])\r\n            ans = (ans + stp[i]*(p1 - p2)) % mod\r\n        return ans",
    "java": "// Runtime: 178 ms (Top 61.28%) | Memory: 109.5 MB (Top 34.27%)\r\nclass Solution {\r\n    public int totalStrength(int[] strength) {\r\n        int mod = 1000000007;\r\n\r\n        int len = strength.length;\r\n\r\n        long[] prefix = prefixSum(strength, len, mod);\r\n\r\n        Deque<Integer> stack = new ArrayDeque<>();\r\n        stack.push(-1);\r\n\r\n        long ans = 0;\r\n        for(int i = 0; i < len; i++) {\r\n            while(stack.peek() != -1 && strength[i] <= strength[stack.peek()]) {\r\n                int mid = stack.pop();\r\n                int left = stack.peek() + 1;\r\n                int right = i - 1;\r\n\r\n                int n = (mid - left);\r\n                int t = (right - mid);\r\n\r\n                long val = (1l * (1 + n) * (prefix[right + 2] - prefix[mid + 1]) + mod) % mod;\r\n                val -= (1l * (1 + t) * (prefix[mid + 1] - prefix[left]) + mod) % mod;\r\n                val *= strength[mid];\r\n\r\n                ans += val;\r\n                ans %= mod;\r\n            }\r\n\r\n            stack.push(i);\r\n        }\r\n\r\n        int right = len - 1;\r\n        while(stack.peek() != -1) {\r\n            int mid = stack.pop();\r\n            int left = stack.peek() + 1;\r\n\r\n            int n = (mid - left);\r\n            int t = (right - mid);\r\n\r\n            long val = (1l * (1 + n) * (prefix[right + 2] - prefix[mid + 1]) + mod) % mod;\r\n            val -= (1l * (1 + t) * (prefix[mid + 1] - prefix[left]) + mod) % mod;\r\n            val *= strength[mid];\r\n\r\n            ans += val;\r\n            ans %= mod;\r\n        }\r\n\r\n        return (int)((ans + mod) % mod);\r\n    }\r\n\r\n    private long[] prefixSum(int[] strength, int len, int mod) {\r\n        long[] prefix = new long[len + 1];\r\n\r\n        for(int i = 0; i < len; i++) {\r\n            prefix[i + 1] = prefix[i] + strength[i];\r\n        }\r\n\r\n        long[] doublePrefix = new long[len + 2];\r\n        for(int i = 0; i <= len; i++) {\r\n            doublePrefix[i + 1] = (doublePrefix[i] + prefix[i]) % mod;\r\n        }\r\n\r\n        return doublePrefix;\r\n    }\r\n}"
  }
}
