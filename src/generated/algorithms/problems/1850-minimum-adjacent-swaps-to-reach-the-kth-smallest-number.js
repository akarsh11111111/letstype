export default {
  "id": 1850,
  "name": "Minimum Adjacent Swaps to Reach the Kth Smallest Number",
  "difficulty": "medium",
  "premium": false,
  "topic": "algorithms",
  "url": "https://leetcode.com/problems/minimum-adjacent-swaps-to-reach-the-kth-smallest-number",
  "relativeDir": "M/Minimum Adjacent Swaps to Reach the Kth Smallest Number",
  "slug": "1850-minimum-adjacent-swaps-to-reach-the-kth-smallest-number",
  "availableLanguages": [
    "cpp",
    "java",
    "python",
    "javascript"
  ],
  "defaultLanguage": "cpp",
  "lineCounts": {
    "cpp": 36,
    "java": 77,
    "python": 28,
    "javascript": 66
  },
  "languages": {
    "cpp": "// Runtime: 63 ms (Top 28.88%) | Memory: 6.7 MB (Top 20.04%)\r\nclass Solution {\r\npublic:\r\n\r\n    // GREEDY APPROACH\r\n    // min steps to make strings equal\r\n\r\n    int minSteps(string s1, string s2) {\r\n        int size = s1.length();\r\n        int i = 0, j = 0;\r\n        int result = 0;\r\n\r\n        while (i < size) {\r\n            j = i;\r\n            while (s1[j] != s2[i]) j++;\r\n\r\n            while (i < j) {\r\n                swap(s1[j], s1[j-1]);\r\n                j--;\r\n                result++;\r\n            }\r\n            i++;\r\n        }\r\n        return result;\r\n    }\r\n\r\n    int getMinSwaps(string num, int k) {\r\n        string original = num;\r\n\r\n        while(k--) {\r\n            next_permutation(num.begin(), num.end());\r\n        }\r\n\r\n        return minSteps(original, num);\r\n    }\r\n};",
    "python": "# Runtime: 1243 ms (Top 52.9%) | Memory: 16.52 MB (Top 32.4%)\r\n\r\nclass Solution:\r\n    def getMinSwaps(self, num: str, k: int) -> int:\r\n        num = list(num)\r\n        orig = num.copy()\r\n        \r\n        for _ in range(k): \r\n            for i in reversed(range(len(num)-1)): \r\n                if num[i] < num[i+1]: \r\n                    ii = i+1 \r\n                    while ii < len(num) and num[i] < num[ii]: ii += 1\r\n                    num[i], num[ii-1] = num[ii-1], num[i]\r\n                    lo, hi = i+1, len(num)-1\r\n                    while lo < hi: \r\n                        num[lo], num[hi] = num[hi], num[lo]\r\n                        lo += 1\r\n                        hi -= 1\r\n                    break \r\n        \r\n        ans = 0\r\n        for i in range(len(num)): \r\n            ii = i\r\n            while orig[i] != num[i]: \r\n                ans += 1\r\n                ii += 1\r\n                num[i], num[ii] = num[ii], num[i]\r\n        return ans",
    "java": "// Runtime: 34 ms (Top 61.80%) | Memory: 42.9 MB (Top 13.48%)\r\nclass Solution {\r\n    public int getMinSwaps(String num, int k) {\r\n        int[] nums=new int[num.length()];\r\n        int[] org=new int[num.length()];\r\n\r\n        for(int i=0;i<num.length();i++){\r\n            int e=Character.getNumericValue(num.charAt(i));\r\n            nums[i]=e;\r\n            org[i]=e;\r\n        }\r\n\r\n        while(k!=0){\r\n        nextPermutation(nums);\r\n         k--;\r\n        }\r\n\r\n        int ans=0;\r\n\r\n        for(int i=0;i<nums.length;i++){\r\n\r\n            if(nums[i]!=org[i]){\r\n                int j=0;\r\n                for(j=i+1;j<nums.length;j++)\r\n                    if(org[j]==nums[i])\r\n                        break;\r\n\r\n                while(j>0 && j!=i){\r\n                    swap(org,j,j-1);\r\n                    ans++;\r\n                    j--;\r\n                }\r\n\r\n            }\r\n\r\n    }\r\n\r\n      return ans;\r\n\r\n    }\r\n\r\npublic void nextPermutation(int[] nums) {\r\n\r\n        if(nums.length<=1)\r\n            return;\r\n\r\n        int j=nums.length-2;\r\n       while(j>=0 && nums[j]>=nums[j+1])\r\n           j--;\r\n\r\n        if(j>=0){\r\n        int k=nums.length-1;\r\n        while(nums[j]>=nums[k])\r\n            k--;\r\n\r\n        swap(nums,j,k);\r\n\r\n        }\r\n\r\n        reverse(nums,j+1,nums.length-1);\r\n\r\n    }\r\n\r\n    public void swap(int[] nums,int j,int k){\r\n        int temp=nums[j];\r\n            nums[j]=nums[k];\r\n            nums[k]=temp;\r\n    }\r\n\r\n    public void reverse(int[] nums,int i,int j){\r\n        while(i<=j){\r\n            swap(nums,i,j);\r\n                i++;\r\n                j--;\r\n        }\r\n    }\r\n}",
    "javascript": "var getMinSwaps = function(num, k) {\r\n    \r\n    const digits = [...num]\r\n    const len = digits.length;\r\n    \r\n    // helper function to swap elements in digits in place\r\n    const swap = (i, j) => [digits[i], digits[j]] = [digits[j], digits[i]]\r\n    \r\n    // helper function to reverse elements in digits from i to the end of digits\r\n    const reverse = (i) => {\r\n        for (let j = len - 1; i < j; ++i && --j) {\r\n            swap(i, j);\r\n        }\r\n    }\r\n    \r\n    // helper to get the next smallest permutation for digits\r\n    const nextPermutation = () => {\r\n        // from right to left, find the first decreasing index\r\n        // in digits and store it as i\r\n        let i = len - 2;\r\n        while (digits[i] >= digits[i + 1]) {\r\n            i--;\r\n        }\r\n        \r\n        // from right to left, find the first index in digits\r\n        // that is greater than element at i\r\n        let j = len - 1;\r\n        while (digits[j] <= digits[i]) {\r\n            j--;\r\n        }\r\n        \r\n        // swap the 2 elements at i and j\r\n        swap(i, j);\r\n        // reverse all elements after i because we know that\r\n        // all elements after i are in ascending order\r\n        // from right to left\r\n        reverse(i + 1);\r\n    }\r\n    \r\n    // find the next permutation k times\r\n    for (let i = 0; i < k; i++) {\r\n        nextPermutation();\r\n    }\r\n\r\n    // find out how many swaps it will take to get to the\r\n    // kth permutation by finding out how many swaps\r\n    // it takes to put digits back to its original state\r\n    let numSwaps = 0;\r\n    for (let i = 0; i < len; i++) {\r\n        let j = i;\r\n        // find the first element in digits to matches\r\n        // num[i] and hold its place at j\r\n        while (num[i] !== digits[j]) {\r\n            j++;\r\n        }\r\n        \r\n        // move the element at j to i while counting the\r\n        // number of swaps\r\n        while (i < j) {\r\n            swap(j, j - 1);\r\n            numSwaps++;\r\n            j--;\r\n        }\r\n    }\r\n    return numSwaps;\r\n};"
  }
}
