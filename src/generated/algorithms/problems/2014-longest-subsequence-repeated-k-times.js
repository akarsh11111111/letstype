export default {
  "id": 2014,
  "name": "Longest Subsequence Repeated k Times",
  "difficulty": "hard",
  "premium": false,
  "topic": "algorithms",
  "url": "https://leetcode.com/problems/longest-subsequence-repeated-k-times",
  "relativeDir": "L/Longest Subsequence Repeated k Times",
  "slug": "2014-longest-subsequence-repeated-k-times",
  "availableLanguages": [
    "cpp",
    "java",
    "python",
    "javascript"
  ],
  "defaultLanguage": "cpp",
  "lineCounts": {
    "cpp": 48,
    "java": 36,
    "python": 36,
    "javascript": 75
  },
  "languages": {
    "cpp": "class Solution {\r\npublic:\r\n    int Alpha=26;\r\n    bool find(string &s,string &p,int k)\r\n    {\r\n        int j=0;\r\n        int n=s.size();\r\n        int count=0;\r\n        for(int i=0;i<n;i++)\r\n        {\r\n            if(s[i]==p[j])\r\n            {\r\n                j++;\r\n                if(j==p.size())\r\n                {\r\n                    count++;\r\n                    j=0;\r\n                }\r\n                if(count==k)\r\n                {\r\n                    return true;\r\n                }\r\n            }\r\n        }\r\n        return false;\r\n    }\r\n    string longestSubsequenceRepeatedK(string s, int k) \r\n    {\r\n        queue<string>q;\r\n        q.push(\"\");\r\n        string ans=\"\";\r\n        while(q.size())\r\n        {\r\n            auto temp=q.front();\r\n            q.pop();\r\n            for(int i=0;i<Alpha;i++)\r\n            {\r\n                string curr=temp+char('a'+i);\r\n                if(find(s,curr,k))\r\n                {\r\n                    ans=curr;\r\n                    q.push(curr);\r\n                }\r\n            }\r\n        }\r\n        return ans;\r\n    }\r\n};",
    "python": "class Solution:\r\n    def longestSubsequenceRepeatedK(self, s: str, k: int) -> str:\r\n        n = len(s)\r\n        max_chunk_sz = n // k\r\n        \r\n        d = collections.Counter(s)\r\n        chars = sorted([c for c in d if d[c] >= k], reverse=True)\r\n        if not chars:\r\n            return ''\r\n        \r\n        old_cand = chars\r\n        for m in range(2, max_chunk_sz+1):\r\n            new_cand = []\r\n            for t in self.get_next_level(old_cand, chars):\r\n                if self.find(s, t*k):\r\n                    new_cand.append(t)\r\n            \r\n            if len(new_cand) == 0:\r\n                break\r\n            old_cand = new_cand\r\n        return old_cand[0]\r\n        \r\n    def get_next_level(self, cand, chars):\r\n        for s in cand:\r\n            for ch in chars:\r\n                yield s + ch\r\n        \r\n    def find(self, s, t):\r\n        # find subsequence t in s\r\n        j = 0\r\n        for i in range(len(s)):\r\n            if s[i] == t[j]:\r\n                j += 1\r\n            if j == len(t):\r\n                return True\r\n        return False",
    "java": "// Runtime: 405 ms (Top 56.76%) | Memory: 43.2 MB (Top 86.49%)\r\nclass Solution {\r\n    char[] A;\r\n    public String longestSubsequenceRepeatedK(String s, int k) {\r\n        A = s.toCharArray();\r\n        Queue<String> queue = new ArrayDeque<>();\r\n        queue.offer(\"\");\r\n        String ans = \"\";\r\n        int[] count = new int[26];\r\n        BitSet bit = new BitSet();\r\n        for (char ch : A) if (++count[ch-'a'] >= k){\r\n            bit.set(ch-'a');\r\n        }\r\n        while(!queue.isEmpty()){\r\n            String sb = queue.poll();\r\n            for (int i = bit.nextSetBit(0); i >= 0; i = bit.nextSetBit(i+1)){\r\n                String res = sb+(char)(i+'a');\r\n                if (check(k, res)){\r\n                    ans = res;\r\n                    queue.offer(res);\r\n                }\r\n            }\r\n        }\r\n        return ans;\r\n    }\r\n\r\n    private boolean check(int k, String s){\r\n        int cnt = 0;\r\n        for (char ch : A){\r\n            if (s.charAt(cnt%s.length()) == ch && ++cnt >= k * s.length()){\r\n                return true;\r\n            }\r\n        }\r\n        return false;\r\n    }\r\n}",
    "javascript": "// Runtime: 1501 ms (Top 55.56%) | Memory: 58.7 MB (Top 11.11%)\r\n// Idea comes from:\r\n// https://leetcode.com/problems/longest-subsequence-repeated-k-times/discuss/1471930/Python-Answer-is-not-so-long-explained\r\n\r\nvar longestSubsequenceRepeatedK = function(s, k) {\r\n  const freq = {};\r\n  for (const c of s) {\r\n    if (!freq[c]) freq[c] = 0;\r\n    freq[c]++;\r\n  }\r\n\r\n  // Find hot string\r\n  let hot = \"\";\r\n  for (const [c, cnt] of Object.entries(freq)) {\r\n    const repeat = Math.floor(cnt / k);\r\n    hot += c.repeat(repeat);\r\n  }\r\n\r\n  // Find all subset and permutation of hot string\r\n  let targets = new Set();\r\n  const subsets = getSubset(hot);\r\n  for (const subset of subsets) {\r\n    const permutations = getPermutation(subset);\r\n    for (const per of permutations) targets.add(per);\r\n  }\r\n\r\n  // Sort targets by length and lexico\r\n  targets = [...targets].sort((a, b) => {\r\n    const delta = b.length - a.length;\r\n    if (delta) return delta;\r\n    return b.localeCompare(a);\r\n  });\r\n\r\n  // Filter s and check subsequence\r\n  s = [...s].filter(c => hot.includes(c)).join(\"\");\r\n  for (const tar of targets) {\r\n    if (isSubsequence(s, tar.repeat(k))) return tar;\r\n  }\r\n\r\n  return \"\";\r\n};\r\n\r\nfunction getSubset(str) {\r\n  const res = [\"\"];\r\n  for (const c of str) {\r\n    res.push(...res.map((curStr) => curStr + c));\r\n  }\r\n  return [...new Set(res)];\r\n};\r\n\r\nfunction getPermutation(str) {\r\n  let res = [\"\"];\r\n  for (const c of str) {\r\n    const resT = [];\r\n    for (const cur of res) {\r\n      for (let i = 0; i <= cur.length; i++) {\r\n        const curT = cur.substring(0, i) + c + cur.substring(i);\r\n        resT.push(curT);\r\n      }\r\n    }\r\n    res = resT;\r\n  }\r\n  return [...new Set(res)];\r\n};\r\n\r\nfunction isSubsequence(s, t) {\r\n  let i = 0;\r\n  for (let j = 0; j < s.length; j++) {\r\n    if (t[i] === s[j]) {\r\n      i++;\r\n      if (i === t.length) return true;\r\n    }\r\n  }\r\n  return false;\r\n}"
  }
}
