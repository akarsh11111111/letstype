export default {
  "id": 773,
  "name": "Sliding Puzzle",
  "difficulty": "hard",
  "premium": false,
  "topic": "algorithms",
  "url": "https://leetcode.com/problems/sliding-puzzle",
  "relativeDir": "S/Sliding Puzzle",
  "slug": "0773-sliding-puzzle",
  "availableLanguages": [
    "cpp",
    "java",
    "python",
    "javascript"
  ],
  "defaultLanguage": "cpp",
  "lineCounts": {
    "cpp": 64,
    "java": 64,
    "python": 51,
    "javascript": 70
  },
  "languages": {
    "cpp": "// Runtime: 59 ms (Top 21.57%) | Memory: 12.6 MB (Top 21.84%)\r\nclass Node{\r\npublic:\r\nint row;\r\nint col;\r\nvector<vector<int>>state;\r\n    };\r\n\r\nclass Solution {\r\npublic:\r\n\r\nint slidingPuzzle(vector<vector<int>>& board) {\r\n\r\n    vector<vector<int>>target={{1,2,3},{4,5,0}};\r\n    queue<Node>q;\r\n    int n=2;\r\n    int m=3;\r\n    vector<vector<int>>dir={{1,0},{-1,0},{0,1},{0,-1}};\r\n\r\n    for(int i=0;i<board.size();i++)\r\n        for(int j=0;j<board[0].size();j++){\r\n            if(board[i][j]==0){\r\n                q.push({i,j,board});\r\n                break;\r\n            }\r\n        }\r\n\r\n    set<vector<vector<int>>>set;\r\n    set.insert(q.front().state);\r\n    int ladder=0;\r\n    while(!q.empty()){\r\n       int size=q.size();\r\n        for(int i=0;i<size;i++){\r\n            Node curr=q.front();\r\n            q.pop();\r\n            if(curr.state==target)\r\n                return ladder;\r\n\r\n            int row=curr.row;\r\n            int col=curr.col;\r\n            for(auto &x:dir){\r\n                int r=x[0]+row;\r\n                int c=x[1]+col;\r\n\r\n                if(r<n && r>=0 && c<m && c>=0 ){\r\n                    swap(curr.state[r][c],curr.state[row][col]) ;\r\n                    if(set.find(curr.state)==set.end()){\r\n                        set.insert(curr.state);\r\n                        q.push({r,c,curr.state});\r\n                    }\r\n                     swap(curr.state[r][c],curr.state[row][col]) ;\r\n\r\n                }\r\n\r\n            }\r\n\r\n        }\r\n        ladder++;\r\n\r\n    }\r\n    return -1;\r\n\r\n}\r\n};",
    "python": "# Runtime: 93 ms (Top 39.62%) | Memory: 14.1 MB (Top 16.60%)\r\n\r\nclass Solution:\r\n    def slidingPuzzle(self, board: List[List[int]]) -> int:\r\n        def findNei(board):\r\n            directs = [[0, 1], [0, -1], [1, 0], [-1, 0]]\r\n            boards = []\r\n            for i in range(2):\r\n                for j in range(3):\r\n                    if board[i][j] == 0:\r\n                        for dr, dc in directs:\r\n                            tmp = [row.copy() for row in board]\r\n                            r, c = i + dr, j + dc\r\n                            if r in range(2) and c in range(3):\r\n                                tmp[r][c], tmp[i][j] = tmp[i][j], tmp[r][c]\r\n                                boards.append(tmp)\r\n            return boards\r\n\r\n        visited = set()\r\n        target = [[1, 2, 3], [4, 5, 0]]\r\n        if board == target:\r\n            return 0\r\n        rows, cols = len(board), len(board[0])\r\n        q = collections.deque()\r\n        step = 1\r\n\r\n        for row in range(rows):\r\n            for col in range(cols):\r\n                if board[row][col] == 0:\r\n                    boards = findNei(board)\r\n                    for b in boards:\r\n                        if b == target:\r\n                            return step\r\n                        visited.add(tuple([tuple(row) for row in b]))\r\n                        q.append(b)\r\n                    break\r\n\r\n        while q:\r\n            step += 1\r\n            for _ in range(len(q)):\r\n                b = q.popleft()\r\n                boards = findNei(b)\r\n                for b in boards:\r\n                    if b == target:\r\n                        return step\r\n                    t = tuple([tuple(row) for row in b])\r\n                    if t not in visited:\r\n                        visited.add(t)\r\n                        q.append(b)\r\n\r\n        return -1",
    "java": "// Runtime: 22 ms (Top 35.42%) | Memory: 44.4 MB (Top 46.82%)\r\nclass Solution {\r\n    int [][] dir={{1,3},{0,2,4},{1,5},{0,4},{1,3,5},{2,4}};\r\n    public int slidingPuzzle(int[][] board) {\r\n\r\n        String tar=\"123450\";\r\n\r\n        String src=\"\";\r\n\r\n        for(int i =0;i<board.length;i++){\r\n            for(int j=0;j<board[i].length;j++){\r\n                src+=board[i][j];\r\n            }\r\n        }\r\n\r\n       HashSet<String> visited=new HashSet<>();\r\n\r\n        visited.add(src);\r\n        int level=0;\r\n        ArrayDeque<String> q=new ArrayDeque<>();\r\n        q.add(src);\r\n        while(q.size()!=0){\r\n            int t=q.size();\r\n\r\n            while(t-->0){\r\n                String rem=q.remove();\r\n\r\n                if(rem.equals(tar)){\r\n                    return level;\r\n                }\r\n\r\n                int idx=-1;\r\n\r\n                for(int i=0;i<rem.length();i++){\r\n                    if(rem.charAt(i)=='0'){\r\n                        idx=i;\r\n                        break;\r\n                    }\r\n                }\r\n\r\n                for(int i =0;i<dir[idx].length;i++){\r\n                    String str=swapEle(rem,idx,dir[idx][i]);\r\n\r\n                    if(!visited.contains(str)){\r\n                        q.add(str);\r\n                        visited.add(str);\r\n                    }\r\n                }\r\n            }\r\n            level++;\r\n        }\r\n\r\n        return -1;\r\n    }\r\n\r\n    public String swapEle(String rem,int i,int j){\r\n        StringBuilder sb=new StringBuilder(rem);\r\n\r\n        sb.setCharAt(i,rem.charAt(j));\r\n        sb.setCharAt(j,rem.charAt(i));\r\n\r\n        return sb.toString();\r\n    }\r\n}",
    "javascript": "// Runtime: 172 ms (Top 37.35%) | Memory: 51.6 MB (Top 7.23%)\r\n/**\r\n * @param {number[][]} board\r\n * @return {number}\r\n */\r\n\r\nclass BoardState {\r\n    constructor(board, currStep) {\r\n        this.board = this.copyBoard(board);\r\n        this.boardString = this.flatToString(board);\r\n        this.emptyIndex = this.findIndexOf0(board);\r\n        this.currStep = currStep;\r\n    }\r\n    findIndexOf0(board) {\r\n        for(let i = 0; i < board.length; i++) {\r\n            for (let j = 0; j < board[i].length; j++) {\r\n                if(board[i][j] === 0) return [i, j];\r\n            }\r\n        }\r\n        return null;\r\n    }\r\n    copyBoard(board) {\r\n        const newBoard = [];\r\n        board.forEach((row) => {\r\n             newBoard.push([...row]);\r\n        });\r\n        return newBoard;\r\n    }\r\n    flatToString(board) {\r\n        let str = '';\r\n        for(let i = 0; i < board.length; i++) {\r\n            for (let j = 0; j < board[i].length; j++) {\r\n                str += board[i][j];\r\n            }\r\n        }\r\n        return str;\r\n    }\r\n}\r\nvar slidingPuzzle = function(board) {\r\n    let queue = [new BoardState(board, 0)];\r\n    let set = new Set();\r\n    const x = board.length, y = board[0].length;\r\n    const switchMoves = [[1, 0], [0, 1], [-1, 0], [0, -1]];\r\n    let slide = (i, j, newi, newj, currBoardState) => {\r\n        if (newi < 0 || newj < 0 || newi >= x || newj >= y) {\r\n            return null;\r\n        }\r\n        const newBoard = currBoardState.copyBoard(currBoardState.board);\r\n        const temp = newBoard[i][j];\r\n        newBoard[i][j] = newBoard[newi][newj];\r\n        newBoard[newi][newj] = temp;\r\n        const newBoardState = new BoardState(newBoard, currBoardState.currStep + 1);\r\n        return newBoardState\r\n    }\r\n    while(queue.length > 0) {\r\n        const currBoardState = queue.shift();\r\n        set.add(currBoardState.boardString);\r\n        if(currBoardState.boardString === '123450') {\r\n            return currBoardState.currStep;\r\n        }\r\n        const [i, j] = currBoardState.emptyIndex;\r\n        switchMoves.forEach((move) => {\r\n            const newBoardState = slide(i, j, i + move[0], j + move[1], currBoardState);\r\n            if (newBoardState && !set.has(newBoardState.boardString)) {\r\n                queue.push(newBoardState);\r\n            }\r\n        });\r\n    }\r\n    return -1;\r\n};"
  }
}
