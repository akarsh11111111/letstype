export default {
  "id": 2074,
  "name": "Reverse Nodes in Even Length Groups",
  "difficulty": "medium",
  "premium": false,
  "topic": "algorithms",
  "url": "https://leetcode.com/problems/reverse-nodes-in-even-length-groups",
  "relativeDir": "R/Reverse Nodes in Even Length Groups",
  "slug": "2074-reverse-nodes-in-even-length-groups",
  "availableLanguages": [
    "cpp",
    "java",
    "python",
    "javascript"
  ],
  "defaultLanguage": "cpp",
  "lineCounts": {
    "cpp": 61,
    "java": 115,
    "python": 30,
    "javascript": 59
  },
  "languages": {
    "cpp": "// Runtime: 2456 ms (Top 6.39%) | Memory: 329.7 MB (Top 84.60%)\r\nclass Solution {\r\npublic:\r\n\r\n    // Function to reverse a linked list\r\n    ListNode* reverseList(ListNode* head) {\r\n        if(!head)\r\n            return head;\r\n        ListNode* prev = NULL;\r\n        while(head) {\r\n            ListNode* temp = head -> next;\r\n            head -> next = prev;\r\n            prev = head;\r\n            head = temp;\r\n        }\r\n\r\n        return prev;\r\n    }\r\n\r\n    ListNode* reverseEvenLengthGroups(ListNode* head) {\r\n        // Creating a dummy node to avoid adding checks for the first node\r\n        ListNode* dummy = new ListNode();\r\n        dummy -> next = head;\r\n\r\n        ListNode* prev = dummy;\r\n\r\n        // Loop to determine the lengths of groups\r\n        for(int len = 1; len < 1e5 && head; len++) {\r\n            ListNode* tail = head;\r\n            ListNode* nextHead;\r\n\r\n            // Determining the length of the current group\r\n            // Its maximum length can be equal to len\r\n            int j = 1;\r\n            while(j < len && tail && tail -> next) {\r\n                tail = tail -> next;\r\n                j++;\r\n            }\r\n\r\n            // Head of the next group\r\n            nextHead = tail -> next;\r\n\r\n            if((j % 2) == 0) {\r\n                // If even sized group is found\r\n                // Reversing the group and setting prev and head appropriately\r\n                tail -> next = NULL;\r\n                prev -> next = reverseList(head);\r\n                prev = head;\r\n                head -> next = nextHead;\r\n                head = nextHead;\r\n            } else {\r\n                // If group is odd sized, then simply going towards the next group\r\n                prev = tail;\r\n                head = nextHead;\r\n            }\r\n        }\r\n\r\n        // Returning the head\r\n        return dummy -> next;\r\n    }\r\n};",
    "python": "# Runtime: 3864 ms (Top 51.07%) | Memory: 53.5 MB (Top 68.62%)\r\nclass Solution:\r\n    def reverseEvenLengthGroups(self, head: Optional[ListNode]) -> Optional[ListNode]:\r\n        group = 2\r\n        tail = head # tail of previous group\r\n        while tail and tail.next:\r\n            cnt = 1 # actual size of the current group\r\n            cur = tail.next # first node of the current group\r\n            while cur.next and cnt < group:\r\n                cur = cur.next\r\n                cnt += 1\r\n            pre, cur = tail, tail.next\r\n            if cnt % 2 == 0: # if group size is even\r\n                while cnt and cur:\r\n                    nxt = cur.next\r\n                    cur.next = pre\r\n                    pre = cur\r\n                    cur = nxt\r\n                    cnt -= 1\r\n                first = tail.next # first node of the original group\r\n                first.next = cur\r\n                tail.next = pre\r\n                tail = first\r\n            else:\r\n                while cnt and cur:\r\n                    pre, cur = cur, cur.next\r\n                    cnt -= 1\r\n                tail = pre\r\n            group += 1\r\n        return head",
    "java": "// Runtime: 32 ms (Top 20.66%) | Memory: 252.2 MB (Top 80.33%)\r\n// This Question can be solved easily using two standard methods of LinkedList\r\n// 1) addFirst (it adds node in front of the LinkedList)\r\n// 2) addLast (it adds node in end of the LinkedList)\r\n\r\nclass Solution {\r\n\r\n    static ListNode oh;\r\n    static ListNode ot;\r\n    static ListNode th;\r\n    static ListNode tt;\r\n\r\n    public ListNode reverseEvenLengthGroups(ListNode head) {\r\n\r\n        oh = null;\r\n        ot = null;\r\n        th = null;\r\n        tt = null;\r\n\r\n        if(head == null || head.next == null)\r\n            return head;\r\n\r\n        int size = length(head);\r\n        int idx = 1;\r\n        ListNode curr = head;\r\n        int group = 1;\r\n\r\n        while(curr!=null)\r\n        {\r\n            int temp = size - idx + 1;\r\n            if((temp>=group && group%2 == 0) || (temp<group && temp%2 == 0))\r\n            {\r\n                int k = group;\r\n                while(k-->0 && curr!=null)\r\n                {\r\n                    ListNode t = curr.next;\r\n                    curr.next = null;\r\n                    addFirst(curr);\r\n                    curr = t;\r\n                    idx++;\r\n                }\r\n            }\r\n            else\r\n            {\r\n                int k = group;\r\n                while(k-->0 && curr!=null)\r\n                {\r\n                    ListNode t = curr.next;\r\n                    curr.next = null;\r\n                    addLast(curr);\r\n                    curr = t;\r\n                    idx++;\r\n                }\r\n            }\r\n\r\n            if(oh==null && ot==null)\r\n            {\r\n                oh = th;\r\n                ot = tt;\r\n            }\r\n            else\r\n            {\r\n                ot.next = th;\r\n                ot = tt;\r\n            }\r\n\r\n            th = null;\r\n            tt = null;\r\n            group++;\r\n        }\r\n\r\n        return oh;\r\n    }\r\n\r\n    public int length (ListNode head)\r\n    {\r\n        if(head==null) return 0;\r\n        ListNode curr = head;\r\n        int k = 0;\r\n        while(curr!=null)\r\n        {\r\n             k++;\r\n            curr = curr.next;\r\n        }\r\n        return k;\r\n    }\r\n\r\n    public void addFirst(ListNode head)\r\n    {\r\n        if(tt == null && th == null)\r\n        {\r\n            th = head;\r\n            tt = head;\r\n        }\r\n        else\r\n        {\r\n            head.next = th;\r\n            th = head;\r\n        }\r\n    }\r\n\r\n    public void addLast(ListNode head)\r\n    {\r\n        if(tt == null && th == null)\r\n        {\r\n            th = head;\r\n            tt = head;\r\n        }\r\n        else\r\n        {\r\n            tt.next = head;\r\n            tt = head;\r\n        }\r\n    }\r\n}",
    "javascript": "var reverseEvenLengthGroups = function(head) {\r\n    let groupSize = 2;\r\n\r\n    let start = head;\r\n\r\n    let prev = head;\r\n    let curr = head.next;\r\n\r\n    let count = 0;\r\n    \r\n    while (curr != null) {\r\n        if (count === groupSize) {\r\n            if (groupSize % 2 === 0) { // we only reverse when it is even\r\n                const end = curr;\r\n                const tail = start.next; // the starting node of the reverse linked list will be the tail after the reverse takes place\r\n                reverseList(start, end, count); // we need to reverse everything in the middle of start and end \r\n                start = tail; // we set the new start to the end of the reversed linked list\r\n            }\r\n            else { // when groupSize is even we don't need to reverse, but need to set the new start to the prev node\r\n                start = prev;\r\n            }\r\n            count = 0; // whenever we reached the group size we need to reset our count and up our groupSize\r\n            ++groupSize;\r\n        }\r\n        else { // just a normal traversal when we haven't hit our groupSize\r\n            prev = curr;        \r\n            curr = curr.next;\r\n            ++count;\r\n        }\r\n    }\r\n   \r\n    if (count % 2 === 0) { // in the case where we ended early on even count\r\n         reverseList(start, null, count);\r\n    }\r\n    \r\n    return head;\r\n    \r\n    \r\n    function reverseList(start, end, count) {\r\n        if (start.next == null) return start; // for case when we have a single node\r\n        \r\n        let prev = start;\r\n        \r\n        let curr = start.next;\r\n        let tail = start.next;\r\n        \r\n        for (let i = 0; i < count; ++i) {\r\n            const next = curr.next;\r\n            curr.next = prev;\r\n            prev = curr;\r\n            curr = next;\r\n        }\r\n        \r\n        start.next = prev;\r\n        tail.next = end;\r\n        \r\n        return ;\r\n    }\r\n};"
  }
}
